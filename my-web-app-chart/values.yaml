# values.yaml

replicaCount: 2

image:
  repository: masked
  tag: latest

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "250m"
    memory: "256Mi"

service:
  type: LoadBalancer
  port: 80

ingress:
  enabled: false

